# M3 sperf 实验知识点大纲

> 本文件用于梳理 `sperf` 实验涉及的核心知识点。请根据你自己的实现过程，逐项补充说明、关键代码片段和踩坑记录。

---

## 1. 实验目标

- `sperf` 是一个什么样的工具？
- 它最终输出了什么信息？
- 为什么要用 `strace -T` 而不是自己实现系统调用跟踪？

---

## 2. 命令行参数解析

### 2.1 参数结构
- `argc` / `argv` 的含义是什么？
- 本实验中 `argv[0]`、`argv[1]`、`argv[2...]` 分别代表什么？

### 2.2 命令路径解析
- 什么是绝对路径？什么是相对路径？
- `PATH` 环境变量的作用是什么？
- `getenv("PATH")` 返回什么？
- 如何按 `:` 拆分 PATH 中的多个目录？
- `access(path, X_OK)` 判断的是什么？

---

## 3. 进程创建与替换

### 3.1 fork()
- `fork()` 的返回值有哪些可能？分别代表什么？
- 子进程和父进程在 `fork()` 之后各自拥有哪些资源的副本？
- 为什么 `fork()` 之后通常要区分 `pid == 0`、`pid > 0`、`pid < 0` 三种情况？



### 3.2 execve()
- `execve()` 的三个参数分别是什么？
- 为什么调用 `execve()` 之后，如果成功就不会返回？
- `execve()` 失败时通常如何处理？
- 本实验中为什么要执行 `strace -T <cmd>`，而不是直接执行 `<cmd>`？

---

## 4. 管道（Pipe）的建立与重定向

### 4.1 pipe()
- `pipe(pipefd)` 做了什么？
- `pipefd[0]` 和 `pipefd[1]` 分别是什么端？
- 管道是单向还是双向的？

### 4.2 关闭不需要的端
- 子进程为什么要 `close(pipefd[0])`？
- 父进程为什么要 `close(pipefd[1])`？
- 如果不关闭会发生什么？

### 4.3 dup2() 重定向
- `dup2(oldfd, newfd)` 的作用是什么？
- 为什么要用 `dup2` 把 `STDOUT_FILENO` 和 `STDERR_FILENO` 都重定向到管道写端？
- `strace` 默认输出到哪里？为什么要重定向？

### 4.4 完整流程图
请用文字或 ASCII 图描述：
1. 父进程创建管道
2. `fork()` 产生子进程
3. 子进程重定向并执行 `strace`
4. 父进程从管道读取并解析

---

## 5. 进程等待

### 5.1 waitpid()
- `waitpid(pid, &status, 0)` 三个参数的含义？
- 为什么父进程要等待子进程？
- 不等待会发生什么？（僵尸进程 / 无法获取子进程退出码）

### 5.2 状态解析
- `WIFEXITED(status)` 判断什么？
- `WEXITSTATUS(status)` 能拿到什么？
- 本实验中如何利用子进程的退出状态决定父进程的返回值？

---

## 6. strace 输出解析

### 6.1 strace -T 输出格式
- 典型的一行长什么样？
- 系统调用名在哪里？
- 时间戳在哪里？单位是什么？

### 6.2 字符串解析
- 如何提取系统调用名？
- 如何提取耗时？
- 为什么要用 `strrchr` 而不是 `strchr` 找时间？
- 有哪些行需要被跳过？

### 6.3 数据统计
- 如何累计同一种系统调用的总时间？
- 如何计算每个系统调用占总时间的百分比？
- 排序用什么算法？时间复杂度是多少？

---

## 7. 环境变量

- `environ` 是什么？
- 为什么 `execve()` 第四个参数传 `NULL` 会导致问题？
- 本实验中子进程需要哪些环境变量？

---

## 8. 缓冲区安全与边界情况

- `strncpy` 和 `strcpy` 的区别是什么？
- `strncpy(dst, src, n)` 在什么情况下不会自动补 `\0`？
- 如果系统调用名超过 63 个字符会发生什么？
- 如果用户输入的命令路径超过 1023 个字符会发生什么？
- 如何安全地处理这些边界情况？

---

## 9. 跨平台注意事项

- 为什么 macOS 上运行本实验会失败？
- macOS 上替代 `strace` 的工具是什么？
- Docker / WSL 如何解决这个环境差异问题？
- 在不同系统之间混用编译好的二进制为什么会报 `Exec format error`？

---

## 10. 调试与排错记录

请记录你在实现过程中遇到的具体问题、原因和解决方法：

| 现象 | 原因 | 解决方法 |
|------|------|----------|
| 输出空表 | | |
| 程序死循环 | | |
| Exec format error | | |
| ... | | |

---

## 11. 扩展思考

- 如果 `strace` 输出量非常大（比如跟踪一个长时间运行的程序），当前实现会有什么问题？
- 如何改进排序效率？
- 如果要支持 `-n` 参数输出 Top N（而不是固定 Top 5），需要改哪些地方？
